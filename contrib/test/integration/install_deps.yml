- name: install deps
  yum:
    name: "{{packages}}"
  vars:
    packages:
     - centos-release-openshift-origin39
     - centos-release-paas-common

- name: install atomic-openshift-utils
  yum:
    name: atomic-openshift-utils

- name: install device-mapper-devel
  yum: 
    name: device-mapper-devel

- name: install glibc-devel
  yum: 
    name: glibc-devel
